__pycache__/
*.pyc
.venv/
*.log
/media/*
# Previously we re‑included specific media folders (idle, events, random) so they were tracked.
# To avoid committing uploaded media clips at all, remove those exceptions.
/playlists/*
# Ignore the generated current playlist, do not track it

# Ignore the virtual environment created in the project root
venv/
